cc_library(
    name = "asl",
    hdrs = [
        "integers.hpp",
        "meta.hpp",
        "ptr.hpp",
        "utility.hpp",
    ],
    visibility = ["//visibility:public"],
)

[cc_test(
    name = "%s_tests" % name,
    srcs = [
        "%s_tests.cpp" % name,
        "test_types.hpp",
    ],
    deps = [
        ":asl",
    ],
) for name in ["integers", "meta", "ptr"]]
