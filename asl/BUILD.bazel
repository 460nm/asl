cc_library(
    name = "asl",
    hdrs = [
        "integers.hpp",
        "meta.hpp",
        "object.hpp",
        "ptr.hpp",
        "utility.hpp",
    ],
    visibility = ["//visibility:public"],
)

[cc_test(
    name = "%s_tests" % name,
    srcs = [
        "%s_tests.cpp" % name,
    ],
    deps = [
        ":asl",
    ],
) for name in ["integers", "meta", "object", "ptr"]]
